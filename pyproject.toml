[project]
name = "ka-ching"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
	"Flask>=2.2.0",
	"flask-cors>=3.0.10",
	"python-dotenv>=1.0.0",
	"icalendar>=4.0.0",
	"pymongo>=4.0.0",
	"werkzeug>=2.2.0",
	"uvicorn>=0.22.0"
]


[project.optional-dependencies]
dev = [
	"pytest>=7.0",
	"mypy>=0.991"
]

[tool.poetry]
# If you're using poetry this section is ignored by pip but kept for convenience
name = "ka-ching"
version = "0.1.0"
